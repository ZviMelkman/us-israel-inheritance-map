{#
  EMAIL CAPTURE  .  MailerLite embedded form (account 2279891, form AMRXu1).
  The MailerLite Universal script lives once in base.njk, before </head>.
  This form carries its own heading, promise copy, email field and button,
  already styled navy + brass to match the site, so this include is just the
  embed target. To change wording or look, edit the form inside MailerLite.
#}
<div class="capture-embed">
  <div class="ml-embedded" data-form="AMRXu1"></div>
</div>
