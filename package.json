{
  "name": "usisraelinheritancemap",
  "version": "0.1.0",
  "description": "US-Israel Inheritance orientation content site",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
